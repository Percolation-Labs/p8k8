"""
the internal router will be optionally mounted on an internal (network) instance
dor example database services can access this (still with api key) but external api users will not see it

- notifications and cron triggers from database

"""