# Hetzner Secrets — MANAGED BY ESO
#
# All production secrets are now managed by External Secrets Operator (ESO)
# pulling from OpenBao KV v2. This file is no longer applied.
#
# See: manifests/platform/external-secrets/
#
# To add or update a secret:
#   1. Write to OpenBao:  kubectl exec openbao-0 -c openbao -- bao kv put secret/p8/app-secrets KEY=value
#   2. ESO syncs automatically every 1h, or force: kubectl annotate externalsecrets p8-app-secrets force-sync=$(date +%s) --overwrite
#
# To seed all secrets from .env:
#   ./manifests/scripts/seed-openbao.sh --context=p8-w-1
