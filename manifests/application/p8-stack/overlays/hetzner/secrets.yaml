# Hetzner Secrets — replace placeholder values before applying
# Generate base64 values: echo -n "value" | base64
#
# For production, consider using sealed-secrets or external-secrets with Vault.

apiVersion: v1
kind: Secret
metadata:
  name: p8-database-credentials
  namespace: p8
type: Opaque
stringData:
  username: p8user
  password: "CHANGE_ME_DB_PASSWORD"

---
apiVersion: v1
kind: Secret
metadata:
  name: p8-llm-api-keys
  namespace: p8
type: Opaque
stringData:
  openai-api-key: "CHANGE_ME_OPENAI_KEY"
  anthropic-api-key: "CHANGE_ME_ANTHROPIC_KEY"
  auth-session-secret: "CHANGE_ME_SESSION_SECRET_32CHARS"
  auth-google-client-id: ""
  auth-google-client-secret: ""
